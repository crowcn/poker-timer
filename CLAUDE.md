# Poker Timer — 项目规范

## 项目定位

朋友局德州扑克锦标赛盲注计时器。纯前端应用，双击即可使用，零外部依赖。

这是开发目录，不是生产目录。

## 技术约束

- **多文件结构**：`index.html` 为入口，CSS 和 JS 拆分为独立文件（`style.css`、`app.js` 等），通过相对路径引用
- **零依赖**：不使用任何框架、CDN、npm 包
- **浏览器兼容**：Chrome / Edge / Safari 最新版，含移动端
- **中文界面**，代码注释用中文
- **总文件大小**控制在合理范围内，单个文件尽量不超过 50KB

## 目录结构

```
poker-timer/
├── CLAUDE.md          # 本文件 — 项目规范（持久有效，不写时点性内容）
├── PRD.md             # 产品需求文档 — 功能与数据模型的唯一真相来源
├── index.html         # 入口 HTML
├── style.css          # 样式
├── app.js             # 主逻辑
├── db.js              # IndexedDB 数据层
└── CHANGELOG.md       # 变更记录（可选）
```

## 开发规则

### 需求管理
- `PRD.md` 是需求的唯一真相来源（含术语表、数据模型、游戏规则）
- 开发必须符合 PRD 描述，有歧义时先问用户，不要猜
- 如果开发过程中发现 PRD 需要调整，先更新 PRD 再改代码，保持一致

### 代码规范
- HTML / CSS / JS 拆分为独立文件，`index.html` 通过 `<link>` 和 `<script>` 引用
- JS 使用普通 `<script src="...">` 引用（不用 ES Module），避免 `file://` 下的 CORS 限制
- CSS 使用 Custom Properties 管理主题色，方便后续扩展主题
- 布局使用 CSS Grid / Flexbox
- JS 使用原生 ES6+，保持简洁
- 音效用 Web Audio API 合成，不依赖外部音频文件

### 倒计时精度
- 不依赖 `setInterval` 的触发间隔计时
- 用 `Date.now()` 时间戳差值计算剩余时间，`requestAnimationFrame` 或 `setInterval` 仅用于刷新显示
- 防止浏览器后台节流导致计时漂移

## 游戏设计原则

当 PRD 存在矛盾、歧义或未覆盖的场景时，按以下原则裁决。每条原则均来自产品负责人的明确决策。

### 原则一：管理员至上

系统是管理员的工具，不是规则引擎。

- 所状态变更（淘汰、复活、结束）必须由管理员手动触发，系统不自动执行任何游戏逻辑
  > "牌桌是一个线下游戏，当线下有需要更新的时候，管理员需要手动触发"
- 奖金分配由管理员最终裁量，系统预设只是参考，管理员可随意修改
  > "管理员手动分配"
- 管理员可以犯错，系统应容错而非防错。用"直接修改当前状态"替代"撤销历史操作"
  > "我们整个牌局核心是靠管理员操作，这意味着管理员可能犯错误，也允许他及时纠正"
  > "不需要撤销功能吧，这意味着所有操作可以重新修改即可"

### 原则二：SNG 封闭赛制

比赛是封闭式 Sit & Go，开赛后参赛名单锁定。

- 比赛开始后不允许新玩家加入
  > "中途不能加人，sng比赛"
- 比赛只能以两种方式结束：①自然淘汰到剩 1 人；②剩余 2-3 人时协商结束
  > "比赛时需要角逐到最后剩下一人，或者三名冠亚季军或者冠亚军协商结束"
- 排名只看最终出局顺序，中途蘑菇复活的淘汰不计入排名

### 原则三：蘑菇是唯一复活手段

不支持 Addon（存活玩家额外买入），复活只能通过蘑菇。

- 蘑菇是全场共享池，先到先得，同一人可多次使用
  > "1人可以多次购买蘑菇（假如他是最先被淘汰三次的玩家，它可以买三次，抢完本场蘑菇）"
- 可选范围是本场比赛所有当前处于"已淘汰"状态的玩家，不限于最近被淘汰的
  > "不是最近而是本次比赛"（纠正"最近被淘汰"的说法）
- 蘑菇复活的筹码量 = 买入筹码，始终一致
  > "都一样，比如200块钱买入10000筹码，蘑菇也是200买10000"

### 原则四：压力递增

盲注设计必须确保比赛有终点。

- 盲注表用尽后继续以倍率递增，不会停留在最高级
  > "盲注level我还是想继续以某种倍率继续涨，这样能保证快速结束。或逼迫选手协商结束"

### 原则五：筹码为本，简化至上

系统追踪筹码量，不追踪真实金额。砍掉一切非必要的专业机制。

- 系统核心实体是筹码，不关注买入的真实金额
  > "不应该关注买入金额（不要管他是多少钱买入的），而是买入筹码"
- 实物筹码只有三种面额：100 / 1,000 / 10,000，盲注表必须适配
  > "我们在线下所配置的筹码只有三种面额，10000、1000、100"
- 砍掉 Chip Up、筹码颜色管理等专业机制，筹码不够大小盲也可以 all-in
  > "chip up不重要，哪怕剩下100也可以继续参加，哪怕不够大小盲也可以all in"

### 原则六：信任优先于防错

朋友局场景下信任管理员判断，不做过度校验。

- 手机尾号冲突不处理，朋友局概率极低
  > "手机号冲突不用管，朋友局很难撞号"
- 意外关闭后提示恢复，而非自动恢复，把决定权交给管理员
  > "提示未完成比赛询问管理员"

### 原则七：完整记录

所有比赛数据必须可追溯。

- 事件日志必须持久化，不仅仅是内存状态
- 历史数据和跨场次统计是 V1 必须功能
  > "我希望系统能够记录和查询历史所有比赛场次的完整数据，也可以交叉查询"

## 部署与发布

- **仓库**：https://github.com/crowcn/poker-timer （public）
- **线上地址**：https://crowcn.github.io/poker-timer/
- **托管**：GitHub Pages，源为 `main` 分支根目录
- **部署机制**：push 到 `main` 后 Pages 自动构建部署，无需手动操作
- **技术形态**：PWA（`manifest.json` + `sw.js` + Service Worker），支持「添加到主屏幕」+ 离线使用
- **更新流程**：改代码 → `git commit` → `git push origin main` → Pages 自动重新部署 → 手机联网重新打开即更新
